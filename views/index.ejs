<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.0/css/all.css"
        integrity="sha384-lZN37f5QGtY3VHgisS14W3ExzMWZxybE1SJSEsQp9S+oqd12jhcu+A56Ebc1zFSJ" crossorigin="anonymous">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <title>Notes</title>
    <style>
        * {
            margin: 0;
            padding: 0;
        }

        body {
            font-family: arial, sans-serif;
            font-size: 100%;
            margin: 3em;
            background: #666;
            color: #fff;
        }

        h2,
        p {
            font-size: 100%;
            font-weight: normal;
            color: black;
        }

        ul,
        li {
            list-style: none;
        }

        ul {
            overflow: hidden;
            padding: 3em;
        }

        ul li {
            text-decoration: none;
            color: #000;
            background: #f6ff7a;
            display: block;
            height: 15em;
            width: 15em;
            padding: 1em;
            -moz-box-shadow: 5px 5px 7px rgba(33, 33, 33, 1);
            /* Safari+Chrome */
            -webkit-box-shadow: 5px 5px 7px rgba(33, 33, 33, .7);
            /* Opera */
            box-shadow: 5px 5px 7px rgba(33, 33, 33, .7);
            -moz-transition: -moz-transform .15s linear;
            transition: -o-transform .15s linear;
            -webkit-transition: -webkit-transform .15s linear;
        }

        ul li {
            margin: 30px;
            float: left;
        }

        ul li h2 {
            font-size: 140%;
            font-weight: bold;
            padding-bottom: 10px;
        }

        ul li p {
            font-family: "Reenie Beanie", arial, sans-serif;
            /* font-size: 180%; */
        }

        ul li:nth-child(even) {
            transform: rotate(4deg);
            -webkit-transform: rotate(4deg);
            -moz-transform: rotate(4deg);
            position: relative;
            top: 5px;
        }

        ul li:nth-child(3n) {
            transform: rotate(-3deg);
            -webkit-transform: rotate(-3deg);
            -moz-transform: rotate(-3deg);
            position: relative;
            top: -5px;
            background: #f26b6b;
        }

        ul li:nth-child(5n) {
            transform: rotate(5deg);
            -webkit-transform: rotate(5deg);
            -moz-transform: rotate(5deg);
            position: relative;
            top: -10px;
            background: #6bbcf2;
        }

        ul li:hover,
        ul li:focus {
            -moz-box-shadow: 10px 10px 7px rgba(0, 0, 0, .7);
            -webkit-box-shadow: 10px 10px 7px rgba(0, 0, 0, .7);
            box-shadow: 10px 10px 7px rgba(0, 0, 0, .7);
            -webkit-transform: scale(1.25);
            -moz-transform: scale(1.25);
            transform: scale(1.25);
            position: relative;
            z-index: 5;
        }

        @media only screen and (max-width: 600px) {
            ul li {
                margin: 20px 0px;
                float: left;
                width: 13em;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="container-fluid">
            <h1 class="text-center text-capitalize pt-5"> Add Notes </h1>
            <hr class="w-25 mx-auto pt-5">
        </div>

        <form action="/notes" method="POST" class="my-4">
            <input type="text" placeholder="Enter Title" name="title" id="title" required class="form-control col mr-2">

            <textarea type="text" rows="5" placeholder="Enter Description" name="Desc" id="Desc"
                class="md-textarea form-control my-4" required></textarea>
            <button type="submit" class="btn btn-success">Add Note</button>
        </form>

        <section>
            <div class="container-fluid">
                <h1 class="text-center text-capitalize pt-3"> Saved Notes </h1>
                <hr class="w-25 mx-auto pt-5">
            </div>

            <ul>
                <% notes.forEach(note => { %>
                <li>
                    <h2><%= note.title%></h2>
                    <p class="card-text"><%= note.description%></p>
                    <div class="text-right py-2">
                        <a href="<%= note._id%>"><i class='far fa-edit mx-1' style="font-size:24px;color:green"></i></a>
                        <!-- <a href ="/speak/<%= note._id %>"><i class='fas fa-download mx-1' style="font-size:24px"></i></a> -->
                        <a href="/download/<%= note._id %>"><i class='fas fa-download mx-1'
                                style="font-size:24px"></i></a>
                        <a href="/delete/<%= note._id%>"><i class='far fa-trash-alt mx-1'
                                style="font-size:24px;color:red"></i></a>
                    </div>

                </li>
                <% }); %>

            </ul>
        </section>

    </div>
</body>

</html>